local neovimConfig = {}

neovimConfig.interface = {
	fullscreen = true;
}

neovimConfig.font = {
	fontFace = "BigBlueTermPlus Nerd Font Mono",
	fontSize = 10,
}

neovimConfig.editor = {
    wrapLines = false,
    tabSize = 4,
    useSpaces = true
}

return neovimConfig
